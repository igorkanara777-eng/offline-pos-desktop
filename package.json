{
  "name": "offline-pos-desktop",
  "version": "0.1.2",
  "private": true,
  "description": "Offline POS desktop app",
  "main": "electron/main.js",
  "type": "commonjs",

  "scripts": {
    "dev": "vite",
    "build:ui": "vite build",
    "install-app-deps": "electron-builder install-app-deps",
    "build:win": "electron-builder --win --x64 -p never"
  },

  "dependencies": {
    "better-sqlite3": "^11.5.0"
  },

  "devDependencies": {
    "@types/node": "^20.11.30",
    "electron": "31.7.7",
    "electron-builder": "24.13.3",
    "typescript": "^5.5.4",
    "vite": "5.4.19"
  },

  "build": {
    "appId": "com.igorkanara.offlinepos",
    "productName": "Offline POS",
    "directories": {
      "output": "dist"
    },
    "files": [
      "app/**",
      "dist/**",
      "electron/**",
      "package.json"
    ],
    "extraMetadata": { "main": "electron/main.js" },
    "asarUnpack": ["node_modules/better-sqlite3/**/*"],
    "win": {
      "target": [
        { "target":
